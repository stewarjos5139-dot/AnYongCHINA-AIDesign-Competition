# 提交说明

**Topic 03 · 多源运营数据「相似度」模糊匹配工具**
参赛者：张涵博 ｜ 提交日期：2026-09-21

---

## 一、本包内容

| 目录 / 文件 | 内容 |
|-------------|------|
| `源码/` | 完整工程，**自包含**。含 `src/`（8 个核心模块）、`main.py`、`gui_app.py`、`build_exe.py`、`requirements.txt`，以及随源码一起的 `团体赛赛道考题/` 数据目录 |
| `标准成果报表/` | 工具运行后自动生成的 `模糊匹配结果_张涵博_<日期>.xlsx`，4 个 Sheet |
| `可执行程序/` | 免安装 exe + 数据目录 + 使用说明（若以 `--no-exe` 生成则附位置说明） |
| `作品说明文档.md` | 架构设计、算法机制、性能数据、加分项说明、环境配置、异常处理机制 |
| `演示视频脚本指南.md` | 录屏提词卡与分镜脚本 |
| `docs/匹配分布图.png` | 《作品说明文档》§5.2 引用的配图 |
| `文件清单.txt` | 包内全部文件路径与大小 |

> `源码/` 下自带一份 `团体赛赛道考题/`，是为了让源码树**解压即可运行** —— `src/config.py` 会把基准目录解析到 `main.py` 所在的一层，少了这个目录，直接跑 `main.py` 会报「文件不存在」。`可执行程序/` 下另有一份，供 exe 使用（exe 采用「外部优先」策略读取同级数据目录，换数据无需重新打包）。

---

## 二、两种运行方式

### 方式一：免安装（推荐评委快速验证）

进入 `可执行程序/`，双击 **`多源运营数据相似度模糊匹配工具.exe`**。

无需 Python 环境，无需安装任何依赖。数据目录与 `output/` 目录会自动就位在 exe 同级。

### 方式二：源码运行

```bash
# 1. 安装依赖
python -m pip install -r 源码/requirements.txt

# 2. 桌面界面
python 源码/gui_app.py

# 3. 或命令行全流程
python 源码/main.py
```

要求 Python 3.10+（开发环境为 3.13.6）。`main.py` 与 `gui_app.py` 需在 `源码/` 目录下运行（脚本会自动把该目录加入模块搜索路径）。

---

## 三、成果数据速览

| 指标 | 本工具结果 | 官方文档口径 |
|------|-----------:|-------------|
| 清洗后精确匹配 | 45 条 | 约 45 条 |
| 匹配成功合计 | 85 条 | 约 83 条 |
| A 系统独有 | 15 条 | 约 17 条 |
| B 系统独有 | 12 条 | 约 14 条 |

**账目对平验证**：85 + 15 = 100（A 系统总记录数）；85 + 12 = 97（B 系统总记录数）。

**关于与文档口径的 2 条差异**：文档 §2.4 给的 `83 / 17 / 14` 是**约数**，用于说明数据规模。本工具在「通用词折叠 + 关键字号保护」（见《作品说明文档》§3.3）修正后比该口径多匹配了 2 对 —— 「上海哔哩哔哩科技有限公司 vs 上海哔哩哔哩有限公司」（写法差异，实为同一主体）与「华润置地有限公司 vs 华润置地（北京）有限公司」（总分机构）。这 2 对经逐条人工核对，确认应判为匹配，属于**规则改进**而非误匹配。

**误匹配与漏匹配均为 0**：赛题 §5 点名的三个陷阱对（中国石油天然气 / 中国建设银行 / 顺丰控股）全部正确判为独有；点名的简称场景（腾讯）正确判为高度匹配。

---

## 四、关键设计一句话摘要

- **清洗**：剔除隐形字符（覆盖全部 Unicode `Cf` 类）→ 全角转半角 → 去空格 → 统一大写，**原始名称列一字不改**
- **打分**：`0.45×ratio + 0.20×partial_ratio + 0.15×WRatio + 0.20×去标点核心相似度`，另可切换 6 种基础算法（`--algo`）
- **防误匹配**：剥离公共前后缀后比对「字号」，把 `中国石油天然气 vs 中国石油化工` 从 79.0 分压到 **39.5 分**，正确判为独有
- **救简称**：包含关系按覆盖率抬分，把 `平安保险（集团）股份有限公司 vs 平安保险（集团）` 抬到 **94.4 分**
- **可解释**：Sheet1「备注」列由 24 个受控标签自动生成，可直接在 Excel 里筛选复核
- **多线程**：GUI 全程跑在 QThread 子线程，实测一次运行发出 9994 次进度信号，界面无冻结

---

## 五、验证方式

```bash
# 离屏自检：装配窗口 → 跑完整流水线 → 驱动 QThread → 全项通过
python 源码/gui_app.py --selftest

# 命令行全流程（含逐项校验打印）
python 源码/main.py --no-progress
```

打包后的 exe 同样支持自检：

```
多源运营数据相似度模糊匹配工具.exe --selftest
```

---

## 六、环境

Windows 11 ｜ Python 3.13.6 ｜ pandas 3.0.5 ｜ openpyxl 3.1.5 ｜ rapidfuzz 3.14.6 ｜ PyQt6 6.11.0 ｜ matplotlib 3.11.2

---

*详细技术说明见《作品说明文档.md》。*
