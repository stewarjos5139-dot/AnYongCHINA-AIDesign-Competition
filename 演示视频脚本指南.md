# 演示视频脚本指南（3–5 分钟）

> **Topic03 多源运营数据「相似度」模糊匹配工具** · 参赛者：张涵博
> 本文件是**录屏时照着念的提词卡**，不是给评委看的文档。

---

## 一、录制前准备

### 1.1 环境检查（录之前跑一遍，别在录的时候翻车）

```bash
# 1. 确认成果目录是干净的，避免"另存为 (2)" 出现在画面里
del /q output\*.xlsx

# 2. 确认桌面程序能起来
python gui_app.py
# 或者直接用打包好的 exe：
dist\多源运营数据相似度模糊匹配工具.exe
```

**必须提前关掉**：
- Excel（否则导出时报文件占用，虽然程序会自动降级另存，但画面不好看）
- 微信 / QQ / 邮件客户端（弹窗会打断录制）
- 其他占用屏幕的窗口

### 1.2 界面准备

| 项目 | 设置 |
|------|------|
| 窗口尺寸 | 拉到 **1440 × 900** 以上（默认尺寸就是它） |
| 左侧栏 | 确认两个路径框已填好考题默认文件 |
| 初始标签页 | 停在「📊 匹配分布图表」（此时显示"运行匹配后在此展示…"） |
| 浏览器/其他 | 全部最小化 |

### 1.3 录制参数建议

- 分辨率：**1920 × 1080**，浏览器/窗口缩放 100%
- 帧率：30 fps 足够
- 鼠标：开启"点击高亮"，让评委看清操作落点
- 音频：先试录 10 秒回放，确认无电流声再正式开始

---

## 二、分镜脚本

总时长目标 **4 分钟**（允许 3–5 分钟）。下表按时间轴给出画面、操作与口播要点。

---

### 镜头 1 ｜开场与工具定位（0:00 – 0:30）

**画面**：桌面程序主窗口，鼠标不动，让评委先看清全貌。

**口播**（约 90 字）：

> 各位评委好，这是 Topic03 多源运营数据相似度模糊匹配工具。
> 审计对账时，企业 ERP 里的客户全称和银行流水里的对方户名写法往往对不上——全半角括号、多余空格、简称全称、错别字，任何一种都会让 VLOOKUP 直接失效。
> 这个工具就是来解决这个问题的。
> 界面分三块：左边是参数配置，右边是可视化和结果预览，全程在后台线程计算，界面不会卡死。

**操作**：鼠标从左侧「① 数据源」缓慢移到右侧图表区，再移回，示意三块布局。

---

### 镜头 2 ｜导入数据（0:30 – 1:00）

**画面**：左侧面板特写。

**口播**（约 100 字）：

> 先导入数据。A 系统是 ERP 客户交易明细，核心对比列是「客户名称」；B 系统是银行流水，核心对比列是「对方户名」。
> 这里已经预载了考题数据，也可以点「选择…」换成任意 Excel 文件，不需要重新打包。
> 程序会自动探测表头位置、校验字段完整性——字段缺失会告警但不会中断。

**操作**：
1. 鼠标划过 A、B 两个路径框（停在框上约 1 秒，让悬浮提示里的"核心对比列"露出来）
2. 点击一次 B 行的「选择…」，弹出文件对话框后 **按 ESC 取消**（示意可换文件，但不真的换）

> 💡 **不要真的换文件**——换成其他文件会导致后面演示的数字和文档对不上。

---

### 镜头 3 ｜调节阈值（1:00 – 1:40）★ 加分项

**画面**：左侧「② 匹配阈值」区域。

**口播**（约 120 字）：

> 阈值可以实时调节。默认是赛题推荐口径：高度匹配 90%、中低匹配 70%、识别下限 60%。
> 超过 90% 判定为可直接确认，70 到 90 之间建议人工复核，低于 60% 则判为该系统独有记录。
> 这里做了约束，三个值必须满足从大到小的关系，非法组合会被直接拦下。
> 点「恢复默认阈值」可以一键回到 90 / 70 / 60。

**操作**：
1. 依次点上、下箭头，把高度阈值调到 **95**（画面显示 95%）
2. 把中低分界线调到 **75**
3. **停顿 1 秒**展示当前组合
4. 点击「↺ 恢复默认阈值（90 / 70 / 60）」，三个值动画式跳回 90 / 70 / 60

> ⚠️ **务必恢复默认再往下走**，否则后面展示的图表和 Excel 数字与文档不符。

---

### 镜头 4 ｜运行匹配（1:40 – 2:20）

**画面**：左侧「③ 执行」区域 → 底部状态栏。

**口播**（约 110 字）：

> 点「开始智能匹配」。
> 注意看，整个计算过程跑在独立的 QThread 子线程里，主界面完全不卡——进度条会实时汇报走到哪一步，日志框里同步打印读取、清洗、匹配、导出的每一步。
> 考题数据是 100 条对 97 条，全量笛卡尔积 9700 对，整个流程耗时在 0.2 秒以内。
> 匹配完成后会弹出结果摘要。

**操作**：
1. 点击「▶ 开始智能匹配」
2. **在弹窗出现前**，快速把鼠标移到右侧「📝 运行日志」标签上（先不点）
3. 弹窗出现后，停留 **2 秒**让评委看清摘要，点「OK」

---

### 镜头 5 ｜展示环形图与四色表格（2:20 – 3:20）★ 核心亮点

**画面**：右侧图表页 → 切到结果预览页。

**口播**（约 180 字）：

> 先看图表。
> 左边是**对账全局环形图**，统计基数是两个系统去重后的全部 114 条参与记录——而不是只看 A 系统的 100 条，否则 B 系统独有的 14 条会在图上凭空消失。
> 环心是全局对账成功率 72.8%。三片分别是成功匹配 83 条、A 系统独有 17 条、B 系统独有 14 条。
> 右边是细分档位，完全匹配 45 条、高度匹配 38 条，另外两根柱子是两边的独有记录——**0 值的档位也保留在图上**，保证图表和控制台、Excel 的数字能逐项对上。
>
> 切到结果预览。83 条记录全部展开，**每种匹配状态一个颜色**：完全匹配是绿色，高度匹配是青色，中低匹配是琥珀色，低置信度是橙色。
> 最有价值的是最右边的「备注」列——这不是自由文本，而是**受控词表的可解释性标签**。比如这条「包含关系（附加地区）（B多出「(广州)」）」、这条「疑似错别字（「限」vs「线」）」。
> 审计师可以直接在 Excel 里按标签筛选，批量处理需要复核的记录。

**操作**：
1. 鼠标沿环形图顺时针绕一圈，停在环心「72.8%」上
2. 依次划过右侧 6 根柱子（在「B系统独有 14」上停顿稍久）
3. 点击「📋 结果预览（前 100 条）」标签
4. 拖动滚动条向下滚约 20 行，展示绿/青两色相间的行
5. 鼠标**悬停在某一行的备注格上**，让完整 tooltip 浮出来（停 2 秒）
6. 滚回顶部

---

### 镜头 6 ｜导出 Excel 成果（3:20 – 4:00）★ 评分硬指标

**画面**：左侧「💾 导出完整报表…」按钮 → 弹出的文件对话框 → 打开生成的 Excel。

**口播**（约 130 字）：

> 最后导出成果报表。
> 报表严格对照官方模板，四个 Sheet：模糊匹配结果 83 行、A 系统独有记录 17 行、B 系统独有记录 14 行。
> 83 加 17 正好是 100 条 A 记录，83 加 14 是 97 条 B 记录——**两侧账目完全对平**。
> 格式上，表头蓝底白字加粗、冻结首行、自动筛选、金额千分位；完全匹配和高度匹配是浅绿底，中低匹配浅黄底，独有记录红色字体。
> 另外程序会自动把原始名称、清洗后名称、相似度分数全部保留，方便回溯核对。

**操作**：
1. 点击「💾 导出完整报表…」
2. 在保存对话框里 **直接点保存**（不要改路径和文件名）
3. 用资源管理器打开 `output/` 文件夹，双击生成的 xlsx
4. Excel 打开后，**依次点四个 Sheet 标签**，每个停留约 2 秒
5. 停在「匹配统计汇总」页，让评委看清 45 / 38 / 17 / 14 / 83 这几个数字

> 💡 Excel 打开后如果弹出"宏/保护视图"提示，**提前在 Excel 设置里关掉**，别在录制时处理。

---

### 镜头 7 ｜收尾（4:00 – 4:20）

**画面**：切回程序主界面，全景。

**口播**（约 80 字）：

> 以上就是这个工具的完整流程。
> 除了图形界面，它还支持命令行和批量模式，可以一次处理整个文件夹的多个批次数据。
> 打包脚本能一键生成免安装的 exe，换数据不需要重新打包。
> 感谢各位评委观看。

**操作**：鼠标缓慢从图表区移到左上角 logo 区域，画面淡出。

---

## 三、时间分配总览

| 镜头 | 内容 | 起止 | 时长 |
|:----:|------|------|-----:|
| 1 | 开场与工具定位 | 0:00 – 0:30 | 30s |
| 2 | 导入数据 | 0:30 – 1:00 | 30s |
| 3 | **调节阈值**（加分项） | 1:00 – 1:40 | 40s |
| 4 | 运行匹配（多线程） | 1:40 – 2:20 | 40s |
| 5 | **环形图 + 四色表格**（核心） | 2:20 – 3:20 | 60s |
| 6 | **导出 Excel**（评分硬指标） | 3:20 – 4:00 | 40s |
| 7 | 收尾 | 4:00 – 4:20 | 20s |
| | **合计** | | **4 分 20 秒** |

留了 40 秒余量，实际录到 3:30–4:40 都在 3–5 分钟要求内。

---

## 四、口播稿全文（可直接朗读版）

> 各位评委好，这是 Topic03 多源运营数据相似度模糊匹配工具。
>
> 审计对账时，企业 ERP 里的客户全称和银行流水里的对方户名写法往往对不上——全半角括号、多余空格、简称全称、错别字，任何一种都会让 VLOOKUP 直接失效。这个工具就是来解决这个问题的。
>
> 界面分三块：左边是参数配置，右边是可视化和结果预览，全程在后台线程计算，界面不会卡死。
>
> 先导入数据。A 系统是 ERP 客户交易明细，核心对比列是「客户名称」；B 系统是银行流水，核心对比列是「对方户名」。这里已经预载了考题数据，也可以点「选择…」换成任意 Excel 文件，不需要重新打包。程序会自动探测表头位置、校验字段完整性——字段缺失会告警但不会中断。
>
> 阈值可以实时调节。默认是赛题推荐口径：高度匹配 90%、中低匹配 70%、识别下限 60%。超过 90% 判定为可直接确认，70 到 90 之间建议人工复核，低于 60% 则判为该系统独有记录。这里做了约束，三个值必须满足从大到小的关系，非法组合会被直接拦下。点「恢复默认阈值」可以一键回到 90 / 70 / 60。
>
> 点「开始智能匹配」。注意看，整个计算过程跑在独立的 QThread 子线程里，主界面完全不卡——进度条会实时汇报走到哪一步，日志框里同步打印读取、清洗、匹配、导出的每一步。考题数据是 100 条对 97 条，全量笛卡尔积 9700 对，整个流程耗时在 0.2 秒以内。
>
> 先看图表。左边是对账全局环形图，统计基数是两个系统去重后的全部 114 条参与记录——而不是只看 A 系统的 100 条，否则 B 系统独有的 14 条会在图上凭空消失。环心是全局对账成功率 72.8%。三片分别是成功匹配 83 条、A 系统独有 17 条、B 系统独有 14 条。右边是细分档位，完全匹配 45 条、高度匹配 38 条，另外两根柱子是两边的独有记录——0 值的档位也保留在图上，保证图表和控制台、Excel 的数字能逐项对上。
>
> 切到结果预览。83 条记录全部展开，每种匹配状态一个颜色：完全匹配是绿色，高度匹配是青色，中低匹配是琥珀色，低置信度是橙色。最有价值的是最右边的「备注」列——这不是自由文本，而是受控词表的可解释性标签。比如这条「包含关系（附加地区）（B多出「(广州)」）」、这条「疑似错别字（「限」vs「线」）」。审计师可以直接在 Excel 里按标签筛选，批量处理需要复核的记录。
>
> 最后导出成果报表。报表严格对照官方模板，四个 Sheet：模糊匹配结果 83 行、A 系统独有记录 17 行、B 系统独有记录 14 行。83 加 17 正好是 100 条 A 记录，83 加 14 是 97 条 B 记录——两侧账目完全对平。格式上，表头蓝底白字加粗、冻结首行、自动筛选、金额千分位；完全匹配和高度匹配是浅绿底，中低匹配浅黄底，独有记录红色字体。另外程序会自动把原始名称、清洗后名称、相似度分数全部保留，方便回溯核对。
>
> 以上就是这个工具的完整流程。除了图形界面，它还支持命令行模式，方便脚本化调用。打包脚本能一键生成免安装的 exe，换数据不需要重新打包。感谢各位评委观看。

---

## 五、录制备忘（贴屏幕边上）

**必须出现的画面**（缺一个都算漏讲）：
- [ ] 三个阈值的调节过程
- [ ] 「恢复默认阈值」被点击
- [ ] 进度条走到 100%
- [ ] 运行日志里出现「读取 / 清洗 / 匹配 / 导出」四类记录
- [ ] 环形图 + 环心的 72.8%
- [ ] **条形图里那根紫色的「B系统独有 14」柱子**
- [ ] 结果预览的四种颜色（至少要看到三种）
- [ ] 备注列的完整标签文字
- [ ] Excel 四个 Sheet 逐个点开
- [ ] 匹配统计汇总页的数字

**绝对不要出现**：
- ❌ 任何弹窗报错
- ❌ 文件占用提示
- ❌ 桌面上的私人文件 / 聊天窗口
- ❌ 阈值停在非默认值（会导致图表数字与文档不符）

**口播禁忌**：
- 不说"大概""差不多"这类模糊词——所有数字都报准确的
- 不提及其他参赛者或商业软件
- 卡词了就停 2 秒重说，后期剪掉，别嗯嗯啊啊

---

## 六、后期剪辑要点

| 项目 | 建议 |
|------|------|
| 开头 | 加 2 秒标题卡：作品名 + 参赛者 + 日期 |
| 等待段 | 加载/弹窗等待超过 2 秒的，加速 2× |
| 重点画面 | 环形图、四色表格、Excel 汇总页各加一个轻微放大或箭头标注 |
| 字幕 | 全程配中文字幕（评委可能静音初筛） |
| 结尾 | 加 3 秒结束卡：作品名 + 联系方式 |
| 导出 | 1080p / H.264 / 8 Mbps 以上，文件名 `Topic03_模糊匹配_张涵博_演示视频.mp4` |

---

## 附录：万一出问题

| 现象 | 原因 | 处理 |
|------|------|------|
| 导出时报文件占用 | Excel 开着同名文件 | 关掉 Excel；程序会自动另存为 `…(2).xlsx`，画面里说明一句即可 |
| 程序启动慢（exe） | 单文件 exe 需解包，约 3–5 秒 | 录之前先启动一次预热，正式录时保持已打开状态 |
| 图表中文变方框 | 系统缺微软雅黑 | 换一台机器，或在 `gui_app.py` 的 `font.sans-serif` 里加 `SimHei` |
| 表格某列看不清 | 窗口太窄 | 拉到 1440×900 以上；列宽可手动拖拽 |
| 录到一半弹更新提示 | 系统/软件自动更新 | 录制前开飞行模式（离线也能跑） |
